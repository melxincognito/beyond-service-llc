import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#801313",
    },
    secondary: {
      main: "#ff8f00",
    },
  },
});

export default theme;
